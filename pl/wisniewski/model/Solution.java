package pl.wisniewski.model;

public class Solution {

    /*
    CREATE TABLE `workshop`.`solution` (
  `id` INT(11) NOT NULL,
  `created` DATETIME NULL,
  `updated` DATETIME NULL,
  `description` VARCHAR(2000) NULL,
  `exercise_id` INT(11) NULL,
  `users_id` BIGINT(20) NULL,
    FOREIGN KEY (`users_id`)
  REFERENCES `workshop`.`users` (`id`)
    FOREIGN KEY (`exercise_id`)
  REFERENCES `workshop`.`exercise` (`id`)
  PRIMARY KEY (`id`));



     */
}
