package pl.wisniewski.model;

public class Exercise {
    /*
    CREATE TABLE `workshop`.`exercise` (
            `id` INT(11) NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(255) NULL,
  `description` VARCHAR(2000) NULL,
    PRIMARY KEY (`id`));
*/
}
